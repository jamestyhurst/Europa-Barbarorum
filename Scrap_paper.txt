1. Insert into existing file
in Main_V2.py
...

    print("Make your turn decision")
    ...
    print([#] Attack Owned Province)

    if turn_choice == #:
        if Player.provinces is None:
            print("You must first claim a province")
        else:
            Player.attack_province()

2. Define first function in separate file

    def attack_province(self)
        option_number = 0
        for province in ProvinceArray
            option_number += 1
            print(f"[{option_number}] {province['name']} , owner: {province['owner']},"
                         f"income: {province['income']}, population: {province['population']}")

        print("Choose a province (or press x to return to previous menu)")
        province_selection = input()
        if province_selection == '#' and ProvinceArray[#-1]['owner'] is None:
            action_loop = True
            selected_province = ProvinceArray[#-1]
            print(f"The Province of {ProvinceArray[#-1]['name']} has been selected")

        elif province_selection == 'x':
            print("Exiting Menu")
            action_loop = False
            choice_loop = False
        else:
            print("Please Try Again")
            action_loop = False

        
        while action_loop == True:
            print(f"Make your decision, you have {Player.treasury} gold")
            print("[1] Seize this province (50 gold)")
            print("[x] Exit this menu")
            province_choice = input()
                if province_choice == '1':
                military.battle(Player, province['owner'])
               
            elif province_choice == 'x':
                print("Exiting this menu")
                action_loop = False
            else:
                print("Please Try Again")

3. Define new function in new file called "Military_V1":

class Battle:

    def __init__(self, side1, side2)
        self.side1 = side1
        self.side2 = side2

        print(f"A battle has begun begun between {side1.name} and {side2.name}")
        ...

